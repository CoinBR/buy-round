version: "3.4"

services:

  db:
    image: eventstore/eventstore:latest
    environment:
      - EVENTSTORE_INSECURE=true # let eventstore run without certificate
      - EVENTSTORE_EXT_TCP_PORT=1113 # set internal tcp port as number
      - EVENTSTORE_HTTP_PORT=2113 # set internal admin ui port as number
      - EVENTSTORE_ENABLE_ATOM_PUB_OVER_HTTP=true
    ports:
      - ${EVENTSTORE_TCP_PORT}:1113 # map internal tcp port as number
      - ${EVENTSTORE_HTTP_PORT}:2113 # map internal admin ui port as number
    volumes:
      - ./db/eventstore/${ENV}/main/data:/var/lib/eventstore
      - ./db/eventstore/${ENV}/log:/var/log/eventstore
